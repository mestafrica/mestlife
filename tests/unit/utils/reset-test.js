import reset from 'mestlife/utils/reset';
import { module, test } from 'qunit';

module('Unit | Utility | reset');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reset();
  assert.ok(result);
});
